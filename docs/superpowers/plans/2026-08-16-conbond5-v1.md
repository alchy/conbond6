# conbond5 v1 — implementační plán

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Konverzační systém s grafovou pamětí, který každou českou větu
zapíše jako výrok s epistemickým stupněm a nad pamětí hodnotí výroky
(ANO/NE/NEVÍM s důkazem a zdrojem), měřený na korpusu conBond2.

**Architecture:** Rozbor z UDPipe (`oracle`) → tabulkové čtení, kde
každý token skončí v roli/atributu/vnoření/částici/zbytku (`read`,
`chronos`) → grafová paměť výroků a uzlů s proveniencí a stupněm
(`memory`) → shoda dotazu s výroky přes uzávěry (`logic`) + propad
aktivací (`recall`) → dialog a žurnál (`dialog`) → české šablony
(`render`) → měření (`bench`). Spec:
`docs/superpowers/specs/2026-08-16-conbond5-design.md`.

**Tech Stack:** Python 3.11, `networkx`; dev `pytest`, `mypy`. UDPipe
služba `cb-udpipe` na `127.0.0.1:42200` (conBond3), model
`cs_all-ud-2.17-251125`; testy hermetické přes nahrané rozbory (JSON).

## Global Constraints

- Python `>=3.11`; jediná běhová závislost `networkx>=3.0`.
- Docstringy česky, bohaté: proč / co / vstup / výstup (preference J.).
- Persistence pouze JSON, deterministické id (`s0001`, `e0001`, `g0001`,
  `p0001`, `t0001`, `o0001`), žádné hodiny ani neseedovaná náhoda; „teď“
  = číslo tahu.
- Nikde brána zápisu: přečtená věta se zapíše vždy; nepřečtené = zbytek
  + otevřená položka.
- Pravdivost nikdy neteče po měkké hraně; aktivace jen řadí.
- Testy nesahají na síť: rozbory jsou nahrané v `tests/data/parses.json`
  (klíč = text věty), pořizuje je `python -m cb6.record` proti službě.
- Každý test čtení hlídá „nic se neztrácí“: každý token má právě jedno
  místo určení.

---

## Struktura souborů

| soubor | odpovědnost |
|---|---|
| `cb6/oracle.py` | `Token`, `Parse`, `UDPipeOracle` (handshake, `parse`, `segment`), `CachedOracle` (JSON keš na disku), `RecordedOracle` (testy) |
| `cb6/chronos.py` | čas: `TimeSpec` (rok/den/interval), rozpoznání z podstromu (den. měsíc rok, roku N, v letech A–B, závorka), `before`, `within` |
| `cb6/read.py` | `Reading`, `Predication`, `RoleFill`, `TermSpec`, `read(parse, mood) -> Reading` — tabulkové čtení, zbytek |
| `cb6/defaults.py` | tabulky výchozích voleb jako data: role z předložky+pádu+druhu výplně, determinátory → kvantifikátor, částice, tázací slova → díra, modální slovesa, časová substantiva |
| `cb6/memory.py` | `Node`, `Statement`, `Role`, `OpenItem`, `Provenance`, `Memory` (attach/revoke/inspect/save/load, uzávěry, aktivace, `graph()`) |
| `cb6/ground.py` | `Reading` → uzly a `Statement`y (identita entit, group, místa, časy, koreference přes aktivaci) |
| `cb6/logic.py` | `Query`, `Verdict`, `Proof`, `evaluate(memory, query)`, `enumerate_(memory, query)`, `describe(memory, node)` |
| `cb6/recall.py` | `recall(memory, nodes, k) -> list[Statement]` propad aktivací |
| `cb6/render.py` | šablony `cs`, `render_answer`, `render_statement`, `render_term` |
| `cb6/dialog.py` | `Session`: `ingest`, `say`, `open`, `resolve`, žurnál, `replay` |
| `cb6/bench.py` | běh nad korpusem conBond2 + QA přesnost + rozklad chyb; `python -m cb6.bench` |
| `cb6/record.py` | pořízení nahraných rozborů pro testy |
| `cb6/cli.py` | `python -m cb6 chat` / `ingest` / `ask` |
| `tests/` | `test_oracle.py`, `test_chronos.py`, `test_read.py`, `test_memory.py`, `test_ground.py`, `test_logic.py`, `test_recall.py`, `test_render.py`, `test_dialog.py`, `test_dialogues_af.py`, `test_adversarial.py`, `test_replay.py`, `data/parses.json` |

---

### Task 1: Kostra projektu + oracle + nahrané rozbory

**Files:** `pyproject.toml`, `README.md`, `cb6/__init__.py`, `cb6/oracle.py`, `cb6/record.py`, `tests/data/parses.json`, `tests/test_oracle.py`

**Interfaces (Produces):**
```python
@dataclass(frozen=True) class Token: index:int; form:str; lemma:str; upos:str; head:int; deprel:str; feats:tuple[tuple[str,str],...]
    def feat(name)->str|None
@dataclass(frozen=True) class Parse: text:str; tokens:tuple[Token,...]; provenance:str
    def root()->Token; def children(i)->tuple[Token,...]; def subtree(i)->tuple[Token,...]; def token(i)->Token
class OracleError(RuntimeError); class OracleUnavailable(OracleError)
class UDPipeOracle: __init__(endpoint="http://127.0.0.1:42200", model_fallback="cs_all-ud-2.17-251125"); parse(text)->Parse; segment(text)->tuple[Parse,...]; provenance:str
class CachedOracle(inner, path: Path)  # JSON keš {text: parse}, zapisuje průběžně
class RecordedOracle(path: Path)       # jen z keše; chybějící věta -> KeyError s návodem
def parse_from_json(d) -> Parse; def parse_to_json(p) -> dict
```
- [ ] `pyproject.toml` (name conbond5, deps networkx, dev pytest+mypy, pytest testpaths), venv `.venv` (python3.11), `pip install -e .[dev]`.
- [ ] `oracle.py`: transport přes `urllib`; handshake `/version` doplní chybějící `model` z `model_fallback` (služba conBond3 ho neuvádí) a zapíše to do `provenance` s příznakem `model?=`; `parse` = `/v1/parse` `{"text":…}` → první věta (víc vět → `SegmentationError`), `segment` → všechny věty (text = rekonstrukce z tvarů; před interpunkcí bez mezery).
- [ ] `record.py`: `python -m cb6.record tests/data/sentences.txt tests/data/parses.json` — pořídí/doplní rozbory (jedna věta na řádek). Věty pro testy: seznam v Task 3–9 (každý task přidá své).
- [ ] Testy: `RecordedOracle` vrací `Parse` s kořenem; `parse_to_json/parse_from_json` round‑trip; `CachedOracle` netáhne službu podruhé (fake inner počítá volání).
- [ ] Commit `feat: kostra, oracle s keší a nahrané rozbory`.

### Task 2: Chronos — čas jako data

**Files:** `cb6/chronos.py`, `tests/test_chronos.py`

**Interfaces (Produces):**
```python
@dataclass(frozen=True) class TimeSpec: start:tuple[int,int,int]|None; end:tuple[int,int,int]|None; label:str; kind:"point"|"year"|"interval"|"name"
def time_from_tokens(tokens: Sequence[Token]) -> TimeSpec|None   # „23. srpna 1851“, „roku 1851“, „v roce 1851“, „1851“, „(1851–1930)“ → interval, „v letech 1910–1920“, „v pondělí“ → kind=name label=pondělí
def is_time_noun(lemma) -> bool   # rok, léta, století, den, měsíc, doba, období, věk, pondělí…neděle, jaro…zima + měsíce
def before(a: TimeSpec, b: TimeSpec) -> bool|None   # None = nesrovnatelné
def within(a: TimeSpec, b: TimeSpec) -> bool|None   # bod v intervalu / rok v roce
def year_of(t) -> int|None
MONTHS = {"leden":1,…}  # lemmata
```
- [ ] Testy z reálných tvarů: `[23., srpna, 1851]` → (1851,8,23); `[roku, 1851]`; `[v, roce, 1851]`; `[1851, –, 1930]` → interval; `[v, letech, 1910, –, 1920]`; `[v, pondělí]` → name; `before((1851..),(1930..))` True; `within(point 1851-08-23, year 1851)` True; `year_of`.
- [ ] Implementace čistě nad lemmaty/tvary tokenů (bez regexů nad textem věty).
- [ ] Commit.

### Task 3: Čtení — jádro predikace (VERB kořen, role, negace, modalita, zbytek)

**Files:** `cb6/defaults.py`, `cb6/read.py`, `tests/test_read.py`, věty do `tests/data/sentences.txt`

**Interfaces (Produces):**
```python
Quant = Literal["∀","∃","·"]
@dataclass class TermSpec: head:int; lemma:str; forms:tuple[str,...]; upos:str; kind:"entity"|"group"|"place"|"time"|"value"|"pron"|"wh"; attrs:tuple[str,...]; count:int|None; time:TimeSpec|None; gender:str|None; number:str|None; quant:Quant|None; quant_authority:str; tokens:tuple[int,...]; name_tokens:tuple[int,...]
@dataclass class RoleFill: name:str; surface:str; terms:list[TermSpec]; authority:str; nested:"Predication|None"; wh:bool
@dataclass class Predication: pred:str|None; kind:"verb"|"copula"|"fragment"; neg:bool; modality:str|None; kernel:str|None; roles:list[RoleFill]; mood:"assert"|"question"; head:int; tokens:tuple[int,...]; defaults:list[str]; secondary:list["Predication"]  # vztažné/acl, koordinace přísudků, nmod výroky
@dataclass class Reading: parse:Parse; main:Predication; residue:list[tuple[str,str]]  # (form, deprel-cesta)
    def placement()->dict[int,str]   # index -> "pred"|"role:kdo"|"attr"|"nested"|"particle"|"residue"|"punct"|"secondary"
def read(parse: Parse, mood: str|None=None) -> Reading
```
`defaults.py`:
```python
ROLE_BY_CASE: dict[tuple[str,str], dict[str,str]]  # (prep, Case) -> {"place":"kde","time":"kdy","*":"v+Loc"}  ; obsahuje v+Loc, na+Loc, do+Gen, z+Gen, od+Gen, k+Dat, s+Ins, o+Loc, po+Loc, u+Gen, za+Gen/Acc/Ins, před+Ins, mezi+Ins, přes+Acc, na+Acc, pro+Acc, bez+Gen, podle+Gen, během+Gen, holé Ins→čím, holé Dat→komu, holé Gen→čeho, holé Acc→jak_dlouho (u času) / co
DETERMINER_QUANT = {"každý":"∀","všechen":"∀","veškerý":"∀","žádný":"∀neg","ten":"·","tento":"·","tenhle":"·","onen":"·","nějaký":"∃","některý":"∃","jeden":"∃","jistý":"∃"}
PARTICLES = {"ne","také","též","i","jen","pouze","už","již","ještě","asi","prý","však","ale","tedy","totiž","například","zejména","hlavně","především","přece","snad","přitom","dnes"?…}  # dnes NE — je čas
MODAL_VERBS = {"moci":"možnost","muset":"nutnost","smět":"možnost","mít":"povinnost"(jen s xcomp Inf),"lze":"možnost","dát_se":"možnost"}
WH = {"kde":("kde",),"kam":("kam",),"odkud":("odkud",),"kdy":("kdy",),"kdo":("kdo",),"co":("co",),"kolik":("count",),"jaký":("attr",),"který":("attr",),"proč":("advcl:protože",),"čí":("nmod",),"jak":("jak",)}
```
- [ ] Testy (každý ověří i `placement()` — žádný token bez místa, kromě `punct`):
  - „Petr bydlí v Praze.“ → pred `bydlet`, kdo=Petr(entity, ·), kde=Praha(place, surface `v+Loc`, authority default), defaults obsahuje `role:kde<-v+Loc`.
  - „Alois Jirásek se narodil ve východočeském Hronově u Náchoda.“ → pred `narodit_se`, kdo entity name_tokens (1,2), kde Hronov attrs (východočeský), zbytek prázdný, sekundární výrok `nmod:u+Gen(Hronov, Náchod)`.
  - „Tučňák nelétá.“ → neg True, kdo group tučňák ∀ [default: generický prézens].
  - „Chov domácích zvířat může mít negativní dopad na jejich zdraví, pokud nejsou splněny určité požadavky.“ → pred `mít`, modality `možnost`, kdo=chov (nmod zvíře[domácí] jako sekundární `nmod:Gen`), co=dopad[negativní] (nmod `na+Acc` zdraví sekundární; `jejich` det → attr/pron), role `advcl:pokud` nested pred `splněný` neg True co=požadavek[určitý].
  - „Petr jel v pondělí do Prahy.“ → kdy=pondělí (time name), kam=Praha.
  - „Dospělý pes má 42 zubů.“ → kdo pes[dospělý] ∀, co zub count 42 ∃.
  - „Celý život pracoval jako učitel dějepisu na gymnáziu, nejprve v Litomyšli a poté v Praze.“ → kdo chybí (pro‑drop → RoleFill kdo terms=[] authority `prodrop`), kde=[gymnázium, Litomyšl, Praha], role `jako`=učitel, `obl:Acc`=život[celý], `jak`=[nejprve, poté] nebo částice; zbytek prázdný.
  - Otázky: „Kde se narodil Alois Jirásek?“ mood question, role kde wh=True; „Kolik zubů má dospělý pes?“ co=zub wh count; „Kdy se narodil Isaac Newton?“ kdy wh.
- [ ] Implementace `read.py`: `_predicate(root)` (VERB; modální+xcomp; `expl:pv`; aux Polarity; `advmod` ne → neg), `_roles(head)` podle deprelů, `_term(token)` (PROPN+flat → entity; NOUN → group s amod attrs, det → quant, nummod → count, time noun / NUM → time přes chronos; NameType=Geo nebo předložka místa + PROPN → place; PRON → pron s rodem/číslem; wh), `_obl_role(token)` → tabulka, `_flatten_conj` (conj pod slovesem s `case` → další obl; conj pod nominálem → víc termů v roli; conj VERB → sekundární predikace se sdíleným podmětem), `_nested(token)` pro xcomp/ccomp/csubj/advcl (hloubka 1; hlouběji → zbytek), `_secondary` pro acl/acl:relcl (predikace o hlavě: relativizátor → role s termem hlavy), nmod → sekundární `nmod:<p+Pád>`; kvantifikátor podle tabulky: subjekt holý NOUN + Tense=Pres + Aspect=Imp → ∀ default; jinak subjekt · default; předmět ∃ default; det přebije (authority determiner); PROPN ·.
- [ ] Zbytek: každý nezařazený token (mimo částice/punct/case/cc/mark/det/aux) → `(form, "deprel>deprel…")` a `placement()` = residue.
- [ ] Commit `feat: čtení predikace — role, negace, modalita, zbytek`.

### Task 4: Čtení — kopula, fragment, životopisná závorka, koordinace přísudků

**Files:** `cb6/read.py`, `tests/test_read.py`, věty do `sentences.txt`

- [ ] Testy:
  - „Jezevčík je pes.“ → kind copula, pred `být`, kernel `subset` [default], kdo=jezevčík ∀, co=pes.
  - „Je jezevčík pes?“ (root AUX, `pes` jako `nmod` Nom pod podmětem) → totéž jako výše, mood question.
  - „Každý spisovatel je člověk.“ → kernel subset, kdo ∀ authority determiner.
  - „Žádný stroj není člověk.“ → kernel subset, neg True, kdo ∀ (žádný).
  - „Praha je v Česku.“ → kernel `within`, kdo=Praha place, kde=Česko place.
  - „Hrabal je spisovatel.“ → kernel `member` [default: PROPN podmět].
  - „Alois Jirásek (23. srpna 1851 Hronov – 12. března 1930 Praha) byl český prozaik, dramatik, středoškolský učitel, a politik.“ → main copula member Jirásek → co=[prozaik[český], dramatik, učitel[středoškolský], politik]; secondary: `narodit_se(kdo, kdy=1851-08-23, kde=Hronov)` a `zemřít(kdo, kdy=1930-03-12, kde=Praha)` s defaults `bio-závorka`; zbytek prázdný.
  - „Kdo je Isaac Newton?“ → copula question, wh role co (kdo jako díra), kdo=Newton.
  - „Úrazy způsobené pády.“ → kind fragment, termy zapsané (úraz s acl způsobený → sekundární predikace `způsobený(co:úraz, čím:pád)`), pred None.
  - „Petr přišel a sedl si.“ → main `přijít`, secondary `sednout_si` se sdíleným kdo=Petr.
- [ ] Implementace: `_copula(root)`: root NOUN/ADJ/PROPN s `cop`, nebo root AUX `být` s nsubj + (nmod Nom | obl); kernel: ADJ → `jaký` role, žádný kernel; NOUN predikát: podmět PROPN nebo det ·/entity → member; group → subset; predikát s `case` v/na+Loc a místem → within; `_bio_parenthesis(subject)`: parataxis/nmod řetěz uvnitř závorky za jménem → chronos + PROPN Geo → dvě sekundární predikace; `_fragment(root)`.
- [ ] Commit `feat: kopula, fragment, životopisná závorka, koordinace přísudků`.

### Task 5: Paměť — graf výroků

**Files:** `cb6/memory.py`, `tests/test_memory.py`

**Interfaces (Produces):**
```python
Grade = Literal["said","read","derived"]
@dataclass class Node: id:str; kind:str; names:list[str]; lemma:str; attrs:list[str]; base:str|None  # base = id základní group u zúžené
    time:TimeSpec|None; count:int|None
@dataclass class Role: name:str; terms:list[str]; quant:Quant|None; authority:str; surface:str
@dataclass class OpenItem: id:str; kind:str; about:str; question:str; options:list[str]; answer:str|None; statement:str
@dataclass class Provenance: doc:str; sent_no:int; text:str; turn:int; model:str
@dataclass class Statement: id:str; pred:str|None; kind:str; neg:bool; modality:str|None; kernel:str|None; roles:list[Role]; grade:Grade; defaults:list[str]; residue:list[tuple[str,str]]; open:list[str]; prov:Provenance; status:str; reason:str; derived_from:str|None; sentence:str  # id uzlu věty
class Memory:
    def new_node(kind, lemma, names=(), attrs=(), base=None, time=None) -> Node   # id deterministicky
    def find_entity(name_lemmas: Sequence[str]) -> list[Node]   # scelení jmen: podmnožina/nadmnožina slov
    def find_group(lemma, attrs=()) -> Node|None; def ensure_group(lemma, attrs=()) -> Node  # zúžená group dostane `restricts` hranu → base
    def ensure_place(lemma) -> Node; def ensure_time(spec) -> Node
    def attach(stmt: Statement) -> Statement   # přidělí id, zapíše hrany role/source, u kernel i jádrovou hranu; vrací s id
    def revoke(sid, reason) -> list[str]        # i derived_from potomky
    def revoke_utterance(sentence_id, reason)
    def inspect(sid) -> Statement; def active() -> list[Statement]; def statements_about(node_id) -> list[Statement]
    def subset_star(a,b)->list[str]|None; def member_star(e,g)->list[str]|None; def within_star(a,b)->list[str]|None; def before(a,b)->bool|None; def same_as_star(a,b)->list[str]|None; def disjoint(a,b)->str|None
    def add_open(item: OpenItem) -> OpenItem; def open_items() -> list[OpenItem]
    def activate(node_ids, energy=1.0); def tick(); def activation(node_id)->float; def most_active(kind=None, gender=None, number=None) -> list[Node]
    def co_mention(node_ids: Sequence[str])   # měkké hrany
    def graph() -> nx.MultiDiGraph
    def save(path); @classmethod load(path); def to_json()->dict; @classmethod from_json(d)
    def program() -> list[str]   # čitelný výpis výroků
```
- [ ] Testy: deterministická id; `ensure_group("mazlíček",("domácí",))` → uzel s `base` a `subset_star` na `mazlíček` dává důkaz s hranou `restricts`; `attach` + `revoke` (potomek `derived_from` odvolán, historie zůstává); `find_entity(["Jirásek"])` najde uzel „Alois Jirásek“; `member_star` přes `subset` řetěz; `within_star`; aktivace vyhasíná po `tick` (0.5×), `most_active(gender="Masc")`; `save/load` round‑trip identický JSON; `graph()` má uzly a hrany typů `role:kdo`, `member`, `co_mention` (soft=True).
- [ ] Commit `feat: paměť — graf výroků, uzávěry, aktivace, JSON`.

### Task 6: Zakotvení — čtení → paměť

**Files:** `cb6/ground.py`, `tests/test_ground.py`

**Interfaces (Produces):**
```python
@dataclass class Grounded: statements:list[Statement]; nodes:list[Node]; notes:list[str]; open:list[OpenItem]
def ground(reading: Reading, memory: Memory, prov: Provenance, grade: Grade, *, topic: str|None) -> Grounded  # ZAPÍŠE do memory
def resolve_term(term: TermSpec, memory, *, discourse_topic) -> tuple[str, list[str]]  # node id, defaults
```
Pravidla: entity → `find_entity` (1 kandidát → scelení; >1 → nejaktivnější, otevřená položka `reference`; 0 → nový uzel se jménem = řetěz `flat` lemmat i tvarů); group → `ensure_group(lemma, attrs)`; place → `ensure_place`; time → `ensure_time`; pron / pro‑drop → `most_active` se shodou rodu/čísla, jinak `topic`, jinak role zůstane s prázdnými termy + otevřená položka `reference`; role s povrchovým jménem (`v+Loc`) → otevřená položka `role_name`; zbytek → otevřená položka `residue`; každý zapsaný výrok aktivuje své termy a `co_mention`; sekundární predikace se zapíší jako vlastní výroky se `derived_from=None`, ale se stejným `sentence`.
- [ ] Testy: „Petr bydlí v Praze.“ → 1 výrok, uzly Petr(entity) Praha(place), role kde; „Alois Jirásek se narodil…“ + druhá věta „Jirásek zemřel v Praze.“ → týž uzel; pro‑drop věta po větě s Jiráskem → kdo=Jirásek default `prodrop:aktivace`; „On se oženil.“ → totéž; věta s neznámou rolí `přes+Acc` → otevřená položka `role_name`; fragment → výrok kind fragment.
- [ ] Commit.

### Task 7: Logika — hodnocení výroků

**Files:** `cb6/logic.py`, `tests/test_logic.py`

**Interfaces (Produces):**
```python
@dataclass class Proof: statements:list[str]; steps:list[str]; defaults:list[str]; grade:Grade
@dataclass class Verdict: value:Literal["ANO","NE","NEVÍM","KONFLIKT","MOŽNÁ"]; proofs:list[Proof]; missing:list[str]; fillers:list[tuple[str,Proof]]  # pro wh: node id + důkaz
def evaluate(memory, q: Predication, qnodes: dict[int,str]) -> Verdict     # ano/ne
def enumerate_(memory, q, qnodes) -> Verdict                                 # wh
def describe(memory, node_id) -> list[Statement]                             # kdo/co je X
def matches(memory, q_role: Role, f_role: Role) -> list[str]|None            # kroky nebo None
```
Shoda: predikát rovný (kopula: kernel test přímo uzávěry, tj. „Je jezevčík pes?“ → `subset_star`/`member_star`; „Je Praha v Česku?“ → `within_star`); polarita; každá role dotazu má protějšek; term‑shoda podle spec § 6; modalita; NE z opačné polarity / disjoint / neg kernel; KONFLIKT když obojí; NEVÍM s `missing` („chybí: role kde u bydlet(Petr)“). Wh: `enumerate_` projde aktivní výroky s predikátem a nedíravými rolemi, díra → fillers; `count` → součet `count` na termu nebo počet členů. `describe`: member/subset výroky + být + výroky s uzlem v `kdo`.
- [ ] Testy nad ručně postavenou pamětí (bez parseru): Petr bydlí v Praze → „Bydlí Petr v Praze?“ ANO, „Bydlí Petr v Brně?“ NEVÍM s missing; ∀pes štěká + jezevčík ⊆ pes → „Štěká jezevčík?“ ANO důkaz [subset, ∀]; „Štěká Alík?“ + member(Alík,jezevčík) → ANO přes member_star; ∃ fakt × ∀ dotaz → NEVÍM; neg fakt → NE; disjoint stroj∦člověk + Hrabal ∈ spisovatel ⊆ člověk → „Je Hrabal stroj?“ NE; modality; wh kde → fillers; kolik → 42; describe Newton → member fyzik/matematik/astronom.
- [ ] Commit.

### Task 8: Propad, render, dialog

**Files:** `cb6/recall.py`, `cb6/render.py`, `cb6/dialog.py`, `tests/test_recall.py`, `tests/test_render.py`, `tests/test_dialog.py`

**Interfaces (Produces):**
```python
def recall(memory, node_ids: Sequence[str], k=3) -> list[Statement]
def render_answer(memory, verdict: Verdict, q: Predication, recalled: list[Statement]) -> str
def render_statement(memory, s: Statement) -> str  # „narodit_se(kdo:Alois Jirásek, kde:Hronov, kdy:23. 8. 1851) [přečteno; kde<-v+Loc výchozí] — „…“ (dok, v6)“
@dataclass class Turn: no:int; kind:"ingest"|"say"|"resolve"|"command"; text:str; doc:str|None
@dataclass class Answer: verdict:Verdict; text:str; statements:list[str]
class Session:
    def __init__(self, memory: Memory, oracle, *, journal_path=None)
    def ingest(self, text: str, doc: str) -> list[dict]     # zpráva za větu: {"text","statements","residue","open"}
    def say(self, text: str) -> Answer|list[Statement]      # ? → Answer; jinak zápis grade said; „Ne, …“/„To není pravda.“ → odvolá poslední + zapíše; „Ne každý X.“ → přepis kvantifikátoru
    def open(self) -> list[OpenItem]; def resolve(self, item_id, value) -> Statement
    def command(self, line) -> str   # !zapomeň sX, !role v+Loc = kde, !otevřené, !program, !ulož cesta, !pravidlo jet(kam:X) => být(kde:X), !výjimka létat pták tučňák
```
Můstkové pravidlo (`Memory.add_rule(Rule(src_pred, dst_pred, role_map))`)
čte `logic.evaluate` jako jeden krok navíc: dotaz na `dst_pred` se zkusí
i jako dotaz na `src_pred` s přemapovanými rolemi; důkaz cituje pravidlo
(grade `said`). Dialog A a D toho užijí.
```python
    def journal(self) -> list[Turn]; @classmethod replay(cls, journal, oracle) -> "Session"
```
- [ ] Testy: recall vrátí výroky o aktivních uzlech seřazené; render pro ANO/NE/NEVÍM/wh obsahuje verdikt, zdrojovou větu a doložku stupně; dialog: `ingest` dvou vět + `say("Kde se narodil Alois Jirásek?")` → „Hronově/Hronov“ v textu a `verdict.fillers`; `say("Petr bydlí v Praze.")` + `say("Bydlí Petr v Brně?")` → NEVÍM; oprava „Ne, Petr bydlí v Brně.“ → první odvolán s důvodem, druhý zapsán; `!role přes+Acc = kudy` naučí výchozí a otevřená položka zmizí; replay dává týž `program()`.
- [ ] Commit.

### Task 9: Dialogy A–F, adversariální sada, determinismus

**Files:** `tests/test_dialogues_af.py`, `tests/test_adversarial.py`, `tests/test_replay.py`, věty do `sentences.txt`

- [ ] Dialog A (můstek): „Auto je dopravní prostředek.“ „Dopravní prostředek jezdí po dálnici.“ „Jezdí auto po dálnici?“ → ANO přes subset [read]. („Jak rychle…“ = mez v1: NEVÍM s tím, co ví.)
- [ ] Dialog B: „Citron je ovoce.“ „Ovoce obsahuje vitamíny.“ „Vitamín C je vitamín.“ „Obsahuje citron vitamín C?“ → NEVÍM (∃ ≠ každý), „Obsahuje citron nějaký vitamín?“ → ANO.
- [ ] Dialog C: sylogismus + „Žádný stroj není člověk.“ → „Je Hrabal stroj?“ NE; „Napsal Postřižiny spisovatel?“ ANO svědek Hrabal.
- [ ] Dialog D: „Petr jel v pondělí do Prahy.“ „V úterý jel Petr do Brna.“ „Praha je v Česku.“ „Byl Petr v pondělí v Česku?“ → ANO (kam ⊆ přes within) — pozn.: „být v“ dotaz se páruje na `jet kam` jen přes naučené pravidlo; v1: NEVÍM + recall (mez řečená) — test dokumentuje.
- [ ] Dialog E: „Ptáci létají.“ „Tučňák je pták.“ „Létá tučňák?“ ANO [∀ default]; „Tučňák nelétá.“ → KONFLIKT hlášen, oprava: `!výjimka létat pták tučňák` → ∀ zúženo; „Létá vrabec?“ ANO, „Létá tučňák?“ NE.
- [ ] Dialog F: „Filip má auto.“ „Filipovo auto je modré.“ „Co má Filip?“ → auto [modrý].
- [ ] Adversariální: „Petr bydlí v Praze.“ → „Bydlí Petr v Brně?“ NEVÍM; „Vesmír se rozšířil do dnešní podoby.“ + „Paralelní vesmír je vesmír.“ → „Rozšířil se paralelní vesmír?“: kdo=vesmír je · (minulý čas, perf.) → NEVÍM; „Pes štěká.“ „Štěká jezevčík?“ bez subset → NEVÍM + recall.
- [ ] Replay: žurnál → `program()` shodný.
- [ ] Commit.

### Task 10: Bench nad korpusem conBond2 + CLI + README

**Files:** `cb6/bench.py`, `cb6/cli.py`, `README.md`

- [ ] `bench.py`: klonuje conBond2 do `data/corpus/` (pokud není), pro `--dok X` (nebo všechny): `ingest` textu (řádky → věty službou; keš rozborů `data/cache/parses.json`), spočítá míru zápisu (věty / výroky / role na výrok / podíl tokenů ve zbytku / otevřené na větu), položí otázky z `otazky.json` (dok) + `etalon/conbond` (dok/src) a vyhodnotí (očekávaný řetězec nebo jeho lemma mezi jmény/lemmaty fillerů, u času rok v `TimeSpec`), rozklad chyb (`není výrok s predikátem`, `predikát je, chybí role`, `role je, jiný filler`, `koreference`, `logika`), výstup Markdown + JSON do `mereni/`.
- [ ] `cli.py`: `python -m cb6 chat [--load pam.json]`, `ingest soubor.txt --doc jmeno`, `ask "…"`.
- [ ] README: co to je, tři zásady, spuštění, měření, čísla z prvního běhu.
- [ ] Spustit bench na `alois_jirásek`, `pes_domácí`, `fyzika_gravitace` + celý korpus; zapsat výsledky do README a `mereni/`.
- [ ] Commit `feat: bench, cli, README s prvním měřením`.
